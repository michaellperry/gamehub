# Service IP Environment Configuration

# Server Configuration
NODE_ENV=development
SERVER_PORT=8083

# JWT Configuration
JWT_SECRET=development-secret-key-change-in-production
JWT_EXPIRES_IN=1h
JWT_ISSUER=service-ip
JWT_AUDIENCE=service-clients
JWT_KEY_ID=service-ip-key

# CORS Configuration
CORS_ORIGIN=*

# Logging Configuration
LOG_LEVEL=info

# Client Credentials Directory
# This will be automatically detected in monorepo structure
# CLIENTS_DIR=/path/to/clients/directory